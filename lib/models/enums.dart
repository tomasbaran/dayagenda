enum NavBarSelection {
  unselected,
  calendar,
  list,
  account,
}

enum DayType {
  isToday,
  isTomorrow,
  isYesterday,
  isNoSpecial,
}

enum SheetType {
  newTask,
  updateTask,
}

enum SnackBarType {
  success,
  error,
}
